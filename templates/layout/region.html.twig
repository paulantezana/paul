{#
/**
 * @file
 * Theme override to display a region.
 */
#}
{%
    set classes = [
    'sn-item',
    'region',
    'region-' ~ region|clean_class,
]
%}
{% if content %}
    <div{{ attributes.addClass(classes) }}>
        {{ content }}
    </div>
{% endif %}